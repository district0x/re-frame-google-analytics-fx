(ns re-frame-google-analytics-fx.core-test
  (:require [clojure.test :refer :all]
            [re-frame-google-analytics-fx.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
